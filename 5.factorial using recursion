#include<stdio.h>
int fact(int n)
{
	if(n==0)
	return 1;
    else
	return(n*fact(n-1));
}
int main()
{
	int result,n;
	printf("enter number");
	scanf("%d",&n);
	result=fact(n);
	printf("factorial of %d=%d",n,result);
	return 0;
}
